{
  "name": "quatrix-root",
  "version": "1.0.0",
  "description": "Quatrix - Advanced Game Server Management Panel for CS2",
  "private": true,
  "type": "module",
  "scripts": {
    "setup": "node setup.js",
    "install-all": "node setup.js",
    "dev": "npx concurrently --names \"SERVER,CLIENT\" --prefix-colors \"cyan.bold,magenta.bold\" --prefix \"[{name}]\" \"npm run dev --prefix server\" \"npx wait-on tcp:3001 && npm run dev --prefix client\"",
    "stop": "pkill -f 'tsx' || pkill -f 'vite' || pkill -f 'cs2' || true",
    "build": "npm run build:server && npm run build:client",
    "build:server": "cd server && npm run build",
    "build:client": "cd client && npm run build",
    "start": "cd server && npm start",
    "clean": "rimraf dist && rimraf server/dist && rimraf client/dist",
    "lint": "cd client && npm run lint",
    "fix-client": "rimraf client/node_modules && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "rimraf": "^6.0.1",
    "wait-on": "^9.0.3"
  },
  "engines": {
    "node": ">=20.6.0",
    "npm": ">=8.0.0"
  },
  "keywords": [
    "cs2",
    "game-server",
    "management-panel",
    "rcon",
    "steamcmd",
    "quatrix"
  ],
  "author": "cspamsky",
  "license": "MIT"
}
